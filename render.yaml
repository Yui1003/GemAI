services:
  - type: web
    name: gem-ai-chat
    env: node
    buildCommand: ./render-build.sh
    startCommand: npm start
    rootDir: ./
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
    autoDeploy: true